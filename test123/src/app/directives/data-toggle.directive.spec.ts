import { DataToggleDirective } from './data-toggle.directive';

describe('DataToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new DataToggleDirective();
    expect(directive).toBeTruthy();
  });
});
